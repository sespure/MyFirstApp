# Managing Secrets Locally

To keep sensitive information out of the repository while preserving it locally in the project, we use xcconfig files.

## Setup
1. Copy `Config/Secrets.example.xcconfig` to `Config/Secrets.xcconfig`:
   
